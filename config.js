var config = {
  user : "root",
  password : "", // leave this blank for windows machines
  port: 8889,
  host: "localhost",
  database: "db_flashback",

  kidsmode : false //by default the person loggin in is an adult

}

module.exports = config;
